name: cassandra-network-diff

services:
  mitm-direct:
    image: mitmproxy/mitmproxy:12
    command:
      - mitmdump
      - --mode
      - regular
      - --listen-host
      - 0.0.0.0
      - --listen-port
      - "8080"
      - --set
      - confdir=/mitmproxy
      - -s
      - /capture/mitm_capture.py
    environment:
      CAPTURE_LANE: direct
      CAPTURE_OUTPUT: /captures/direct.jsonl
      CAPTURE_INCLUDE_HOSTS: ${CAPTURE_INCLUDE_HOSTS:-api.anthropic.com}
      CAPTURE_BODY_BYTES: ${CAPTURE_BODY_BYTES:-262144}
    volumes:
      - ./mitm_capture.py:/capture/mitm_capture.py:ro
      - ./captures:/captures
      - mitm_direct_ca:/mitmproxy
    ports:
      - "${DIRECT_MITM_PORT:-18080}:8080"

  mitm-cassandra-client:
    image: mitmproxy/mitmproxy:12
    command:
      - mitmdump
      - --mode
      - reverse:http://cassandra-proxy:8787
      - --listen-host
      - 0.0.0.0
      - --listen-port
      - "8080"
      - -s
      - /capture/mitm_capture.py
    environment:
      CAPTURE_LANE: cassandra-client
      CAPTURE_OUTPUT: /captures/cassandra-client.jsonl
      CAPTURE_INCLUDE_HOSTS: ${CAPTURE_CLIENT_INCLUDE_HOSTS:-mitm-cassandra-client,cassandra-proxy,api.anthropic.com}
      CAPTURE_BODY_BYTES: ${CAPTURE_BODY_BYTES:-262144}
    volumes:
      - ./mitm_capture.py:/capture/mitm_capture.py:ro
      - ./captures:/captures
    ports:
      - "${CASSANDRA_CLIENT_MITM_PORT:-18082}:8080"
    depends_on:
      - cassandra-proxy

  mitm-cassandra-upstream:
    image: mitmproxy/mitmproxy:12
    command:
      - mitmdump
      - --mode
      - regular
      - --listen-host
      - 0.0.0.0
      - --listen-port
      - "8080"
      - --set
      - confdir=/mitmproxy
      - -s
      - /capture/mitm_capture.py
    environment:
      CAPTURE_LANE: cassandra-upstream
      CAPTURE_OUTPUT: /captures/cassandra-upstream.jsonl
      CAPTURE_INCLUDE_HOSTS: ${CAPTURE_INCLUDE_HOSTS:-api.anthropic.com}
      CAPTURE_BODY_BYTES: ${CAPTURE_BODY_BYTES:-262144}
    volumes:
      - ./mitm_capture.py:/capture/mitm_capture.py:ro
      - ./captures:/captures
      - mitm_cassandra_ca:/mitmproxy
    ports:
      - "${CASSANDRA_MITM_PORT:-18081}:8080"

  cassandra-proxy:
    build:
      context: ../..
      dockerfile: Dockerfile
    command: ["--host", "0.0.0.0", "--port", "8787", "--backend", "anthropic"]
    environment:
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:?Set ANTHROPIC_API_KEY}
      ANTHROPIC_TARGET_API_URL: ${ANTHROPIC_TARGET_API_URL:-https://api.anthropic.com}
      HTTPS_PROXY: http://mitm-cassandra-upstream:8080
      HTTP_PROXY: http://mitm-cassandra-upstream:8080
      NO_PROXY: 127.0.0.1,localhost,cassandra-proxy
      REQUESTS_CA_BUNDLE: /mitmproxy/mitmproxy-ca-cert.pem
      SSL_CERT_FILE: /mitmproxy/mitmproxy-ca-cert.pem
    volumes:
      - mitm_cassandra_ca:/mitmproxy:ro
    depends_on:
      - mitm-cassandra-upstream

  claude-direct:
    build:
      context: .
      dockerfile: Dockerfile.runner
      args:
        CLAUDE_CODE_PACKAGE: ${CLAUDE_CODE_PACKAGE:-@anthropic-ai/claude-code}
    profiles: ["run"]
    environment:
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:?Set ANTHROPIC_API_KEY}
      HTTPS_PROXY: http://mitm-direct:8080
      HTTP_PROXY: http://mitm-direct:8080
      NO_PROXY: 127.0.0.1,localhost
      NODE_EXTRA_CA_CERTS: /mitmproxy/mitmproxy-ca-cert.pem
      SSL_CERT_FILE: /mitmproxy/mitmproxy-ca-cert.pem
      CLAUDE_LANE: direct
      CLAUDE_PROMPT: ${CLAUDE_PROMPT:-Summarize this repository in one sentence.}
      CLAUDE_ARGS: ${CLAUDE_DIRECT_ARGS:-}
    volumes:
      - ../..:/workspace:ro
      - mitm_direct_ca:/mitmproxy:ro
    depends_on:
      - mitm-direct

  claude-cassandra:
    build:
      context: .
      dockerfile: Dockerfile.runner
      args:
        CLAUDE_CODE_PACKAGE: ${CLAUDE_CODE_PACKAGE:-@anthropic-ai/claude-code}
    profiles: ["run"]
    environment:
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:?Set ANTHROPIC_API_KEY}
      ANTHROPIC_BASE_URL: http://mitm-cassandra-client:8080
      CLAUDE_LANE: cassandra
      CLAUDE_PROMPT: ${CLAUDE_PROMPT:-Summarize this repository in one sentence.}
      CLAUDE_ARGS: ${CLAUDE_CASSANDRA_ARGS:-}
    volumes:
      - ../..:/workspace:ro
    depends_on:
      - mitm-cassandra-client

volumes:
  mitm_direct_ca:
  mitm_cassandra_ca:
